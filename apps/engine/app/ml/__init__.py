# Phase 19: ML training pipeline package.
# Training scripts live here; they are standalone, manually-invoked scripts
# (like binance_ingest.py and seed_macro_events.py) — never imported from the live engine.
